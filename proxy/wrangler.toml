name = "notion-cors-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Set NOTION_API_KEY in Cloudflare dashboard (Workers & Pages > your worker > Settings > Variables)
# or: wrangler secret put NOTION_API_KEY
